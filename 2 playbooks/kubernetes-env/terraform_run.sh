# To begin with, download and install Terraform.
terraform init

# To create a plan of the resources you want to provision and confirm everything looks okay. 
terraform plan

# Terraform files and build your infrastructure. 
terraform apply

# If you want to tear it down, 
# terraform destroy

# To make changes, simply alter your code and execute terraform apply again. So simple!
# terraform apply